import { PartialEntry, WorkUnit } from "./Timer.models";

export const DEFAULT_TIME = 2400;
export const DEFAULT_ENTRY: PartialEntry = {
  start: -1,
  description: "",
};

export const INITIAL_ENTRIES: WorkUnit[] = [];
